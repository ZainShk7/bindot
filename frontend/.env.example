# Leave unset during `vite` dev to use the proxy in vite.config.ts (avoids CORS).
# Production: use the site origin only — paths already include /api/...
# VITE_API_URL=https://your-app.vercel.app
# Do not set VITE_API_URL to .../api (that duplicates /api in request URLs).

